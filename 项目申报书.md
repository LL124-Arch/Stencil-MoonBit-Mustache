<style>
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;700&display=swap');
@page { margin: 15mm; }
body {
    font-family: 'Noto Sans SC', sans-serif;
    font-size: 12px;
    line-height: 1.4;
}
h1 { font-size: 18px; margin-bottom: 5px; text-align: center; }
h2 { font-size: 15px; margin-top: 10px; margin-bottom: 5px; border-bottom: 1px solid #ccc; padding-bottom: 3px; }
p, ul, ol { margin-top: 3px; margin-bottom: 3px; }
</style>

# 第八届 CCF 开源创新大赛 - 选题申报书

**项目名称：** Stencil - 基于 MoonBit 的轻量级高性能原生模板引擎<br>
**参赛方向：** 生态库移植<br>
**参考规范：** [Mustache 官方核心规范](https://mustache.github.io/mustache.5.html)<br>
**开源协议：** MIT<br>
**申报账号：** LL124-Arch / lll15362196148
**GitHub 仓库：** [https://github.com/LL124-Arch/Stencil-MoonBit-Mustache](https://github.com/LL124-Arch/Stencil-MoonBit-Mustache)
**GitLink 仓库：** [https://www.gitlink.org.cn/lll15362196148/stencil](https://www.gitlink.org.cn/lll15362196148/stencil)
**Mooncakes 包：** [LL124-Arch/stencil](https://mooncakes.io/docs/LL124-Arch/stencil)

## 一、选题背景与意义

**痛点：** 随着 WebAssembly (WASM) 技术的普及，MoonBit 凭借其编译能力与运行时性能适合构建轻量级内容处理组件，但生态中仍缺少可直接复用的原生模板引擎。

**价值：** 本项目完全采用 MoonBit 原生实现 Mustache 风格模板引擎，零外部运行时依赖，支持 WASM、WASM-GC、JS 和 Native 目标，适用于服务端页面、邮件、配置片段、边缘函数和其他结构化文本生成场景。

## 二、核心功能

本项目遵循 Mustache 逻辑分离思想，提供以下可验证功能：

1. **安全插值与防 XSS：** `{{var}}` 默认进行 HTML 实体转义，支持 `{{{var}}}` 和 `{{&var}}` 原始输出。
2. **多态区块渲染：** 根据 JSON 类型处理布尔值、数字、字符串、对象和数组，支持隐式列表迭代、作用域嵌套和点号路径查找。
3. **反转区块处理：** `{{^section}}` 正确处理 `null`、`false`、空数组和空字符串等假值。
4. **局部模板复用：** 支持 `{{>partial}}`、多行智能缩进、嵌套 partial，并对过深展开设置安全上限。
5. **注释与 standalone 行：** 支持 `{{!comment}}`，并处理默认分隔符下的独立注释和区块控制行。
6. **可变分隔符：** 支持 `{{=<% %>=}}<%name%>` 等常见 Mustache 分隔符切换。
7. **可诊断错误：** 对未闭合标签、区块错配、无效分隔符和超深模板返回 `TemplateError`。

当前明确不在范围内的能力包括 lambda、表达式求值、文件系统 partial 加载和动态 partial 名称；完整的参考链接、支持子集和边界见 [Mustache 兼容性说明](docs/mustache-compatibility.md)。

## 三、系统架构设计

Stencil 由四个低耦合模块构成：

1. **Scanner：** 使用游标和字符串查找扫描标签，不依赖正则；支持默认/可变分隔符、三括号原始变量和 standalone 控制行归一化。
2. **Parser：** 使用递归下降构建层次化 AST，统一处理变量、区块、反转区块、partial 和注释，并设置最大区块嵌套深度。
3. **Context：** 使用 JSON 上下文栈实现嵌套作用域、父级回退和点号路径查找，保持数据与模板逻辑分离。
4. **Render：** 遍历 AST，通过 StringBuilder 生成文本，默认安全转义；partial 展开和区块渲染均有深度边界保护。

## 四、工程质量与验证

项目采用 `src` 核心库与 `cli` 示例/基准分包结构，包含：

- 235 个跨功能、错误、兼容性和压力测试；
- WASM、WASM-GC、JS 以及 CI Native 目标验证；
- `moon fmt --check`、`moon check --deny-warn`、多目标 build/info/test；
- GitHub Actions 与 GitLink CI；
- 可复现的编译后重复渲染基准和 CLI 端到端数据；
- README、CHANGELOG、贡献指南、行为准则、许可证和验收自查脚本。

项目发布到 Mooncakes，当前版本和元数据以 `moon.mod`、README 和 Mooncakes 页面为准。

## 五、核心技术优势

1. **MoonBit 原生实现：** 核心逻辑和测试均采用 MoonBit，零第三方运行时依赖。
2. **安全边界明确：** 默认 HTML 转义，并对解析/渲染递归深度进行限制，避免失控输入。
3. **可维护工程流程：** 公开提交记录、双仓库 CI、自动化验收脚本、基准脚本和版本更新日志共同形成可审计证据链。
4. **生态复用价值：** API 小而稳定，可用于 WASM、Native、JS 和边缘文本生成场景，并提供 Mooncakes 包安装方式。
